<h1>Author: <%= @user.name %></h1>
<P><%= @project.name %></P>
<P><%= @project.description %></P>
<p><%= link_to "User", user_path(@user) %>
<p><%= link_to "Edit", edit_user_project_path(@user, @project) %>
<p><%= link_to "Delete", user_project_path(@user, @project),
                         :data => {:confirm => "You sure?",
                                   :method => "delete"} %></p>

